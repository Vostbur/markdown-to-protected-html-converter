📝 Инструкция по использованию программы для создания защищенных HTML-документов
1. Установка необходимых компонентов
Перед первым использованием установите зависимости:
pip install markdown2

2. Подготовка Markdown-файла
Создайте файл с расширением .md и добавьте секретные фрагменты между тегами [hint] и [/hint]:

# Мой документ

Обычный текст виден всем.

[hint]
## Секретный раздел
- Логин: admin
- Пароль: P@ssw0rd
```python
secret_code = "confidential"
```
[/hint]

Другой открытый текст.

#### 3. **Запуск программы**
Выполните в командной строке:
python your_script_name.py

4. Интерфейс программы
Выбор файла:

Нажмите "Browse..." и выберите ваш Markdown-файл

Программа автоматически предложит имя для HTML-файла

Ввод ключа шифрования:

Введите любой ключ (длина не ограничена)

Для показа/скрытия символов используйте чекбокс "Show key"

Сохранение результата:

Укажите путь для сохранения HTML-файла

Нажмите "Convert to Protected HTML"

5. Использование HTML-файла
Откройте полученный файл в браузере

Для просмотра скрытых фрагментов:

Введите ваш ключ в поле "Enter access key"

Нажмите "Submit Key"

Кликайте на ссылки "Show fragment N" для отображения секретного содержимого

6. Особенности работы
🔒 Ключ невозможно восстановить из HTML-файла

✨ Сохраняется все Markdown-форматирование (заголовки, списки, код и т.д.)

📊 Счетчик показывает количество оставшихся скрытых фрагментов

⚠️ При утере ключа просмотр скрытых фрагментов будет невозможен

7. Пример корректного файла

# Отчет о тестировании

[hint]
## Критические уязвимости
1. SQL-инъекция в `/admin.php`
2. XSS в поле комментариев
[/hint]

[hint]
```python
# Код для исправления
def sanitize_input(text):
    return text.replace("'", "''")
```
[/hint]

Остальная часть документа...


#### 8. **Устранение неполадок**
Если возникают проблемы:
1. Проверьте, что в Markdown-файле:
   - Все теги `[hint]` имеют закрывающие `[/hint]`
   - Нет конфликтующих спецсимволов
2. Для сложных случаев:
   - Временно добавьте `print(decryptedText)` в JavaScript-код
   - Проверьте консоль браузера (F12 → Console)

Программа обеспечивает надежную защиту конфиденциальных фрагментов текста с сохранением всего форматирования и простым интерфейсом для конечных пользователей.